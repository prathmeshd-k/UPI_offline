# Server
server.port=${PORT:8091}

# PostgreSQL
spring.datasource.url=${jdbc:postgresql://upimesh_db_jozk_user:PLAIzRGRUayqu03LaMB1FjRW6C14N3s3@dpg-da0190s9v7es738bq1pg-a/upimesh_db_jozk}
spring.datasource.username=${upimesh_db_jozk_user}
spring.datasource.password=${PLAIzRGRUayqu03LaMB1FjRW6C14N3s3}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging
logging.level.com.demo.upimesh=INFO
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# Idempotency window (how long a packet hash is remembered)
upi.mesh.idempotency-ttl-seconds=86400

# Packet freshness window — anything older than this is rejected as replay
upi.mesh.packet-max-age-seconds=86400
